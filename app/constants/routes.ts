/* Centralized route paths to avoid strings across pages and middleware. */
export const ROUTES = {
  home: '/',
  login: '/login',
  onboarding: '/completar-tus-datos',
  profileSuccess: '/perfil-creado',
  dashboard: '/dashboard'
} as const